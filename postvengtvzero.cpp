#include <bits/stdc++.h>
using namespace std;

int main() {
    int num;
    cout<<"enter the number : ";
    cin>>num;
    if(num == 0)
    cout<<"number is zero";
else if(num < 0)
cout<<"number is negative";
else 
cout<<"nummber is positive";
    return 0;
}